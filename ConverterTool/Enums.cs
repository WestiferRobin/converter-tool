namespace ConverterTool
{
    public enum ProgramType
    {
        JAVA = 1,
        PYTHON = 2,
        JAVA_SCRIPT = 3,
        C_SHARP = 4,
        JSON = 5,
        XML = 6,
    }

    public enum LanguageType
    {
        PROGRAM_LANG = 1,
        MARKUP_LANG = 2
    }
}
